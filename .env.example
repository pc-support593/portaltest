# このファイルを「.env」という名前でコピーして値を埋める(.env は git 管理外)
# Entra ID サインインを有効にする設定(docs/entra-setup.md §0 参照)
AUTH_MODE=entra
# アプリ登録の「概要」画面に表示される値(どちらも秘密情報ではない)
TENANT_ID=ここにテナントID(ディレクトリID)
CLIENT_ID=ここにクライアントID(アプリケーションID)
# ポートを変える場合のみ
# PORT=3100
