services:
  - type: web
    name: yoshimura-portal
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: AUTH_MODE
        value: entra
      - key: TENANT_ID
        sync: false
      - key: CLIENT_ID
        sync: false
